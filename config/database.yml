defaults: &defaults
  adapter: sqlite

development:
  database: db/yaeo_development.db
  <<: *defaults

  # Add more repositories
  # repositories:
  #   repo1:
  #     adapter:  postgres
  #     database: sample_development
  #     username: the_user
  #     password: secrets
  #     host:     localhost
  #   repo2:
  #     ...

test:
  database: db/yaeo_test.db
  <<: *defaults
production:
  database: db/yaeo_production.db
  <<: *defaults
